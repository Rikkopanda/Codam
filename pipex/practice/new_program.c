
#include "practice_pipex.h"

int main(int argc, char **argv)
{
    // int fd = open(argv[1], O_WRONLY | O_APPEND);
    // dup2(fd, 1);

    printf("hello from replacing process\n");
    return (0);
}